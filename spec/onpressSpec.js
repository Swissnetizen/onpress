(function () {
  describe("onpress", function () {
    var el;
    beforeEach(function () {
      el = document.createElement("button");
      el.addEventListener("onpress", function () {
    });
    it("should fire when touched")
    it("should fire when clicked")
    it("shouldn't fire when held too long")
    });
  });
}());